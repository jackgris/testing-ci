# Application environment: dev, prod, or test
APP_ENV=dev

# Address where the HTTP server will listen
HTTP_ADDR=:8080

# Timeouts (in seconds)
READ_TIMEOUT_SEC=10
WRITE_TIMEOUT_SEC=10
IDLE_TIMEOUT_SEC=60
